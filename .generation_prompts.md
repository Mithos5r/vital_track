 > Lee los archivos context.md y scope.md de la raíz del proyecto. Confirma que entiendes el stack tecnológico (Flutter, Riverpod,  
   Drift, Health, shared_preferences, firebase) y los criterios de aceptación del MVP. No generes código aún, solo resume           
   brevemente los pilares de la arquitectura que vamos a seguir.        


    > Basado en el context.md, actualiza el archivo pubspec.yaml completo con todas las dependencias necesarias (incluyendo las de     
   drift, freezed y sus respectivos generators). Después, genera crear la estructura de directorios definida en la sección 9 del    
   contexto.        


Vamos a empezar con la logica de auth. Implementa la capa de dominio (UserEntity, excepciones personalizadas, interfaz de AuthRepository) y la capa de datos (FirebaseAuthDataSource, AuthRepositoryImpl, y providers con @riverpod) para el flujo de autenticación siguiendo estrictamente el context.md y scope.md, incluyendo un Stream<User?> para la navegación reactiva con GoRouter, y genera todos los unit tests correspondientes con mocktail para asegurar la cobertura de éxitos y errores según las reglas de negocio definidas.

Haz un flutter pub upgrade y revisa que todas las dependencias funcionan correctamente entre si y vuelve a poner flutter_gen_runner para que funcione correctamente

Implementa la estructura completa de lib/core/ para VitalTrack, incluyendo la configuración de GoRouter con un refreshListenable vinculado al estado de Firebase Auth para gestionar las redirecciones automáticas, el sistema de tematización Material 3 en AppTheme y VitalTrackColors usando el verde #4caf50 como semilla y aplicando los estilos específicos para Card, TextFormField y SnackBar

Basado en el context.md, actualiza el archivo pubspec.yaml completo con todas las dependencias necesarias (incluyendo las de drift, freezed y sus respectivos generators). Después, genera crear la estructura de directorios definida en la sección 9 del contexto.

Implementa la persistencia local con Drift definiendo la tabla health_metrics (punto 10) con user como TEXT para el UID de Firebase, desarrolla el HealthLocalDataSource, la interfaz HealthRepository y su implementación, la entidad inmutable HealthMetricEntity y un AsyncNotifier generado con @riverpod para el Dashboard que consuma un caso de uso de obtención de métricas, incluyendo todos los unit tests para el DataSource y casos de uso utilizando mocktail bajo los principios de Clean Architecture y las reglas de estilo de context.md.

Implementa la pantalla de LoginScreen siguiendo el diseño del punto 8.2 del context.md, creando un AsyncNotifier con Riverpod para gestionar el estado de la autenticación (loading, error, success), integrando los validadores de formulario para email y contraseña, y utilizando AppLocalizations para todos los textos y Assets.logo.svg (vía flutter_gen) para el encabezado; el widget debe disparar los SnackBar personalizados definidos en la sección 7 en caso de error de Firebase y navegar a /register mediante GoRouter, incluyendo un archivo de test exhaustivo que verifique las validaciones de los campos y el comportamiento del botón de acceso.

Implementa la pantalla RegisterScreen siguiendo el punto 8.3 del context.md, utilizando un AsyncNotifier generado con Riverpod para el registro de usuarios, integrando validadores para email, coincidencia de contraseñas y los requisitos de seguridad específicos (6 caracteres, mayúscula, minúscula, número y carácter especial), empleando Assets.logo.svg mediante flutter_gen, textos vía AppLocalizations y el estilo de SnackBar de error de la sección 7 para fallos de Firebase, incluyendo tests unitarios del widget que verifiquen cada regla de validación y la navegación con GoRouter. Cuando finalices crea una rama y sube los cambios.

Revisa el analyze y arregla lo que hicera falta. Despues lanza la aplicación para que pueda testearla en el emulador de ios

Implementa la pantalla de Dashboard (/home) siguiendo el diseño de Bento Box del punto 8.4 del context.md, utilizando un AsyncNotifier generado con Riverpod para cargar las métricas desde la base de datos local y manejando el estado vacío con assets/empty_data_home.svg; añade un Floating Action Button (FAB) expandible que despliegue dos opciones: un icono de "+" para navegar a /add-entry y un icono de logout que ejecute el método de cierre de sesión del AuthRepository, asegurando el uso de AppLocalizations para los textos, flutter_gen para los iconos de las celdas y generando los tests de widget para verificar la expansión del FAB y la correcta visualización de los datos.


Implementa la pantalla AddEntryForm (/add-entry) siguiendo la sección 8.6 del context.md, creando un formulario validado que permita campos vacíos pero asegure que los inputs numéricos sean válidos y que, si se rellena exercise_type o exercise_duration, el otro sea obligatorio; utiliza un AsyncNotifier para persistir los datos en la tabla health_metrics de Drift, emplea AppLocalizations para todos los textos, aplica el estilo de TextFormField definido en la sección 7 y genera tests de widget que confirmen que no se puede guardar un formulario completamente vacío y que las validaciones de ejercicio funcionan correctamente.

Vuelve a la rama main, actualiza, haz un analize, corrige lo que haga falta y mata la app si esta activa.

Haz una revision de context.md y scope.md y verifica que queda por implementar. No escribas nada de código, solo quiero confirmar lo que nos queda para el MVP.

Implementa la pantalla HistoryScreen (/history/:param) siguiendo el punto 8.5 del context.md, utilizando un AsyncNotifier para obtener el listado de métricas desde Drift filtrado por el parámetro de la ruta, implementando un ListView.builder para mostrar cada registro con su valor y DateTime formateado, y asegurando que si el parámetro es exercise se muestre el nombre del ejercicio con su duración debajo; maneja el estado vacío con assets/empty_data_home.svg y el mensaje personalizado de AppLocalizations, e incluye tests de widget para verificar que la lista carga los datos correctos según el parámetro recibido.

En core/constants/health_metrics_info.dart crea una clase sealed que tenga lo basico que se necesita para representar el valor   
   de health_metric como por ejemplo, unit, icon, icon color, background color y localization. Despues crea las clases a partir del 
   sealed para cada uno de los valores del metric y utilizalo en dashboard y en history  

Implementa la SplashScreen (/splash) siguiendo la sección 8.1 del context.md, diseñando una interfaz con fondo verde (#4caf50), el asset assets/splash.gif centrado y el texto "Cargando..." en blanco en la parte inferior; integra la lógica de navegación automática que, al inicializar la app, verifique el estado de sesión en Firebase Auth para redirigir al usuario a /home si está autenticado o a /login en caso contrario, asegurando que el texto esté internacionalizado con AppLocalizations y generando un test de widget que valide la presencia de los elementos visuales y la redirección correcta.

Genera los archivos de configuración para flutter_native_splash y flutter_launcher_icons en assets/native_splash/config.yaml y assets/launcher_icon/config.yaml respectivamente, configurando el icono de la app con assets/logo.png y el splash nativo con fondo verde #4caf50 y la imagen assets/logo_splash.png; además, modifica main.dart para mantener el splash nativo visible con FlutterNativeSplash.preserve() hasta que el RouterNotifier determine la ruta inicial, y actualiza la configuración de GoRouter añadiendo una propiedad errorBuilder que redirija cualquier ruta no definida o inexistente automáticamente a la pantalla de /splash.


Implementa la integración con salud nativa siguiendo el punto 13 del context.md, creando el HealthAppDataSource con el paquete   
   health y el SharedPreferencesDataSource para gestionar el last_sync_timestamp y el flag stop_background_sync; desarrolla el caso 
   de uso de sincronización que verifique el usuario de Firebase, segmente la ventana de tiempo en bloques de 1 hora, realice las   
   medias/sumas de métricas y persista los resultados en la tabla health_metrics de Drift; finalmente, configura Workmanager para   
   ejecutar esta tarea cada hora y añade en el main.dart la lógica para resetear el flag de parada, incluyendo tests unitarios con  
   mocktail que validen la segmentación de tiempo y el filtrado de datos nulos.  


Implementa la funcionalidad de borrado de registros en HistoryScreen añadiendo el método deleteMetric en el HealthLocalDataSource y el HealthRepository, creando un caso de uso específico que sea invocado desde el AsyncNotifier del historial mediante un gesto de "deslizar para borrar" (Dismissible) en cada tile; asegura que la acción actualice el estado local de Riverpod, use AppLocalizations para confirmar la acción y genera los unit tests correspondientes con mocktail para validar el borrado en Drift.

 Procede con la exportación de Json. La exportación se tiene que hacer en bulk todos los datos al mismo tiempo. Desde servidor    
   nos llegará la ultima fecha que tiene que procederemos a exportar de esa fecha hasta el día de hoy      

Realiza el manejo del flag stop_background_sync. El aviso visual tiene que ser un pop indicando que ha habido un error con       
   los permisos, tendra dos botones uno indicando que vaya a setting y otro para cerrar. Revisa que tanto en ios como android se han especificado los permisos necesarios.

   Añade a las acciones del floating action button la opcion de compartir el historio. Este boton compartira el json resultante de la funcionalidad de exportación desde la ultima semana.

      Revisa que puedes ejecutar la app para las siguientes plataformas, iOS, android y web. Si no puedes por un error de configuración en la plataforma arreglalo, en caso de que sea un error en alguno de los packages que estamos usando que te impida ejecutarlo crea un informe de lo que esta pasando.

Crea unos textos para los casos de error de login, ponlos en el archivo de localization y usalos. Lanza los test pertinentes y  Crea una rama con los cambios

Modifica el archivo readme para que conteste:
    Qué es la app y qué hace
    Cómo ejecutarla, y que plataformas funciona actualmente (aunque sea con errores)
    Decisiones técnicas que tomaste y por qué
    Qué herramientas de IA usaste y cómo
    Qué mejorarías con más tiempo

Crea dentro de la carpeta .improves un archivo .md con cosas que podría haber hecho mejor durante el proceso de creación de este MVP. Crea un archivo general_improve.md con un resumen. Luego formatea el .generation_prompts.md y enumera los prompt que hay ahí. Finalmente crea una rama.


# Historial de Prompts (Generación de VitalTrack MVP)

A continuación se enumeran cronológicamente los prompts utilizados durante el desarrollo del proyecto para asegurar la trazabilidad del proceso:

1.  **Alineación Inicial**: Leer archivos `context.md` y `scope.md`, confirmar stack tecnológico y resumir pilares de arquitectura.
2.  **Configuración de Proyecto**: Actualizar `pubspec.yaml` con dependencias (Drift, Freezed, etc.) y crear estructura de directorios Clean Architecture.
3.  **Capa de Autenticación**: Implementar dominio y datos de Auth (Firebase) con navegación reactiva y tests unitarios (`mocktail`).
4.  **Modernización de Dependencias**: Ejecutar `flutter pub upgrade` y restaurar `flutter_gen_runner` resolviendo conflictos de Riverpod 3.x.
5.  **Capa Core (Routing & Theme)**: Configurar `GoRouter` con `refreshListenable` y tematización Material 3 (Verde #4caf50) para Cards y FormFields.
6.  **Persistencia Local**: Implementar tablas Drift, `HealthLocalDataSource`, repositorios y `AsyncNotifier` para el Dashboard con tests.
7.  **Pantalla de Login**: Desarrollar `LoginScreen` con validaciones, i18n, assets y manejo de errores mediante SnackBar.
8.  **Pantalla de Registro**: Implementar `RegisterScreen` con reglas de seguridad de contraseña estrictas y validación de coincidencia.
9.  **Dashboard (Bento Box)**: Crear interfaz de métricas responsiva con FAB expandible y manejo de estados de carga/vacío.
10. **Formulario de Entrada**: Implementar `AddEntryForm` con validaciones de co-dependencia para ejercicios y persistencia en Drift.
11. **Historial de Métricas**: Desarrollar `HistoryScreen` filtrada por tipo, con visualización detallada y navegación interactiva.
12. **Refactorización de Metadatos**: Crear clase `sealed HealthMetricInfo` para centralizar iconos, unidades y colores en toda la app.
13. **Refinamiento Bento**: Ajustar layout de la celda de Ejercicio (ancho completo y posición del tipo de actividad).
14. **Salud Nativa (Health API)**: Implementar integración con HealthKit/Google Fit, segmentación por horas y sincronización en segundo plano.
15. **Arquitectura Background**: Refactorizar lógica de `Workmanager` hacia la capa de datos con abstracciones de dominio.
16. **Optimización de Startup**: Mejorar `main.dart` con manejo de errores fatales y flujo de inicialización resiliente.
17. **Borrado Granular**: Implementar funcionalidad "deslizar para borrar" que limpie propiedades específicas sin eliminar el registro completo.
18. **Ajustes de UI (Dismissible)**: Refactorizar `_HistoryItem` eliminando el `Card` para asegurar alineación perfecta en la animación de borrado.
19. **Exportación JSON**: Crear caso de uso para exportación masiva del historial en formato `snake_case` filtrado por fecha.
20. **Gestión de Errores Nativa**: Implementar diálogo de error de permisos nativos y configuración de `Info.plist`/`AndroidManifest`.
21. **Compartir Historial**: Añadir acción al FAB para compartir el JSON de los últimos 7 días mediante `share_plus`.
22. **Compatibilidad Multiplataforma**: Verificar ejecución en iOS, Android y Web, documentando bloqueadores técnicos.
23. **Localización de Errores**: Internalizar todos los mensajes de excepción de Firebase Auth para soporte multi-idioma.
24. **Limpieza Final**: Auditoría de dependencias, eliminación de paquetes no usados y activos redundantes.
25. **Documentación**: Actualizar `README.md` con retrospectiva técnica, uso de IA y guía de ejecución.
26. **Retrospectiva**: Crear documentación de mejoras en `.improves/` y consolidar el resumen de aprendizaje del MVP.
