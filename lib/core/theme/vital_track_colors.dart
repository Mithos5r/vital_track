import 'package:flutter/material.dart';

class VitalTrackColors {
  VitalTrackColors._();

  static const Color primary = Color(0xFF4CAF50);
  static const Color secondary = Color(0xFF00BCD4); // Celeste
  static const Color background = Color(0xFFFFFFFF);
  
  // Error colors for SnackBar as per context.md
  static const Color errorBorder = Color(0xFFFD574B);
  static const Color errorBackground = Color(0xFFFFF3F0);
  static const Color errorTitle = Color(0xFF830002);
  static const Color errorText = Color(0xFF394352);
}
